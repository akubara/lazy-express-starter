## Description
Simple boilerplate, includes:  
nodemon + express + babel + body-parser + cors + eslint + prettier

## Install and run

``` bash
git clone https://github.com/akubara/lazy-express-starter.git
npm install
npm run 
# ??????? 
# PROFIT

```
